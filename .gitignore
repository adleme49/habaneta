# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies (root and per-workspace)
node_modules
.pnp
.pnp.js

# Build / output (per-workspace)
dist
build
coverage

# Mobile (Expo / RN)
.expo
.expo-shared
android
ios
*.apk
*.ipa

# Tooling caches
.vite
.turbo
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode
.idea

# Local env (per-workspace .env.example is committed)
.env
.env.local
.env.*.local

# Local agent state
.claude
